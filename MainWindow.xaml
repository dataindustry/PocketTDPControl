<Window x:Class="PocketTDPControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PocketTDPControl"
        mc:Ignorable="d"
        MouseDown="Window_MouseDown"
        Closing="Window_Closing"
        AllowsTransparency="False" WindowStyle="None" ResizeMode="NoResize"
        Title="Pocket TDP Control" Height="320" Width="160">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition>
            </RowDefinition>
            <RowDefinition Height="80">
            </RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition>
            </ColumnDefinition>
            <ColumnDefinition>
            </ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Slider
            Grid.Row="0" Grid.Column="0" 
            Orientation="Vertical" TickPlacement="Both"
            TickFrequency="1" HorizontalAlignment="Center" VerticalAlignment="Center" Height="200" Minimum="3" Maximum="33" Value="{Binding CurrentTDP}" />
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" Grid.Column="1">
            <Button Content="3" Width="50" Margin="2" Click="AdjustButton_Click" />
            <Button Content="5" Width="50" Margin="2" Click="AdjustButton_Click" />
            <Button Content="10" Width="50" Margin="2" Click="AdjustButton_Click" />
            <Button Content="15" Width="50" Margin="2" Click="AdjustButton_Click" />
            <Button Content="22" Width="50" Margin="2" Click="AdjustButton_Click" />
            <Button Content="25" Width="50" Margin="2" Click="AdjustButton_Click" />
            <Button Content="30" Width="50" Margin="2" Click="AdjustButton_Click" />
            <Button Content="33" Width="50" Margin="2" Click="AdjustButton_Click" />
        </StackPanel>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0">
            <TextBlock Text="{Binding CurrentTDP}" Width="60" Margin="2" TextAlignment="Center" />
        </StackPanel>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" Grid.Column="1">
            <Button Content="{Binding PresetTDP}" Width="60" Margin="2" Click="AdjustButton_Click" />
            <Button Content="SAVE" Width="60" Margin="2" Click="SaveButton_Click" />
        </StackPanel>
    </Grid>
</Window>
